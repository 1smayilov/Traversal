@if(User.Identity.IsAuthenticated){

<div class="leave-comment-form mt-5" id="reply">
    <h4 class="side-title mb-2">Rəy bildirin</h4>
    <p class="mb-4">
        İşarə qoyulmuş xanaları doldurmalısınız *
    </p>
    <form method="post" asp-action="AddComment" asp-controller="Comment"> @*CommentConroller dəki AddCommentə gedəcək, o metod da ki database ə əlavə edəcək*@
        <input type="hidden" name="DestinationId" value="@ViewBag.destId"/>
        <input type="hidden" name="AppUserId" value="@ViewBag.userId" />
        <div class="form-group">
            <textarea name="CommentContent" class="form-control" placeholder="Rəyiniz *" required="" 
                      spellcheck="false"></textarea> @*Buradakı name databasə dəki table ilə əlaqələnir*@
        </div>
        <div class="submit text-right">
            <button class="btn btn-style btn-primary">Rəy bildir</button>
        </div>
    </form>
    </div>
}
else
{
    <h2>Rəy bildirmək üçün qeydiyyatdan keçin</h2>
}

